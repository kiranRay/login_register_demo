<html>
    <head>
        <title>Register</title>
        <!-- <link rel="stylesheet" href="https://maxcdn.trapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"> -->
        <!-- <script src="https://ajax123.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script> -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
        <body>
            <div class="container">
                <form action="http://localhost:3000/app/register" method="POST">
                    <label for="name">Name</label>
                    <input type="text" name="name" id="name" class="form-control" required><br>
                    <label for="email">E-mail</label>
                    <input type="text" name="email" id="email" class="form-control" required><br>
                    <label for="dob">Date Of Birth</label>
                    <input type="date" name="dob" id="dob" class="form-control" required><br>
                    <label for="gendar">Gendar:</label>
                    <label for="male">Male</label>
                    <input type="radio" name="gendar" id="male" value="Male" class="form-radio-input" required>
                    <label for="female">Female</label>
                    <input type="radio" name="gendar" id="female" value="Female" class="form-radio-input" required><br>
                    <label for="num">Mobile No.</label>
                    <input type="text" name="num" id="num" onkeypress="allowNumbersOnly(event)" class="form-control" maxlength="10" required><br>
                    <label for="pass">Password</label>
                    <input type="password" name="pass" id="pass" class="form-control" required> <br>
                    <label for="conpass"> ConfirmPassword</label>
                    <input type="password" name="conpass" id="conpass" class="form-control" required> <br>
                    <input type="submit" value="Register">
                </form>
                <% if(error){ %> 
                    <ul>
                        <li> <%= error.details[0].message %> </li>
                    <% } %>
                    </ul>
                    <% if(msg){ %> 
                        <ul>
                            <li> <%= msg %> </li>
                        <% } %>
                        </ul>
            </div>
        </body>
    </head>
</html>
<script>
    function allowNumbersOnly(e) {
        var code = (e.which) ? e.which : e.keyCode;
        if (code > 31 && (code < 48 || code > 57)) {
            try{
                e.preventDefault();
            }catch(e){
                console.log('test error....')
            }
        }
    }

    function allowNumbersOnly(e) {
        var code = (e.which) ? e.which : e.keyCode;
        if (code > 31 && (code < 48 || code > 57)) {
            try{
                e.preventDefault();
            }catch{
                console.log('test error....')
            }
        }
    }
</script>